This module adds a two fields in survey.survey form view under options tap:

    - Survey Start Button Name
    - Survey End Heading Name

where you can add the custom text for start button and end message.
