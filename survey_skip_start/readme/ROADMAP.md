- For the moment, it only works with one page layouts.
- If this module is used with a multilingual survey, it 
is not possible to select another language. By default, 
the survey will use the language defined in the context.